import styled from 'styled-components';

export const Button = styled.button`
  padding: 10px 20px;
  margin-left: 10px;
  border: none;

  :hover {
    background-color: aqua;
    transform: scale(1.1);
  }
`;
